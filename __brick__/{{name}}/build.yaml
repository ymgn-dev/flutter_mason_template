targets:
  $default:
    builders:
      json_serializable:
        generate_for:
          include:
            - lib/**/*state.dart
            - lib/features/**/*.dart
        options:
          any_map: true
          explicit_to_json: true
      freezed:
        generate_for:
          include:
            - lib/**/*state.dart
            - lib/features/**/*.dart
